package projects.maaman15.custom_config;

/**
 * This class contains const values for the config labels and the default values inside the config file.
 */
public class CustomConfigConst {
    public static final String UDG_RADIUS_SIZE_LABEL = "UDGRadiusSize";  // The unit disk geometric radius label inside config file
    public static final double DEFAULT_UDG_CONNECTION_DISTANCE = 1.0;  // The unit disk default radius value

    public static final String MINIMUM_NODES_LABEL = "MinimumNodesInGraph";  // The minimum number of nodes label inside config file
    public static final int DEFAULT_MINIMUM_NODES = 1; // The minimum number of nodes default value
}
